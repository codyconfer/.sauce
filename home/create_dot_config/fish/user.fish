# ~/.config/fish/user.fish — your personal fish tweaks.
# chezmoi creates this file once and never overwrites it, so anything you add
# here survives `chezmoi apply`. Sourced at the end of config.fish.
